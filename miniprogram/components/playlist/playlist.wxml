<!--components/playlist/playlist.wxml-->
<view class="playlist-container" bindtap="goTomusiclist">
   <image src="{{playlist.picUrl}}" class="playlist-image"></image>
   <view class="playlist-playcount">{{_count}}</view>
   <view class="playlist-name">{{playlist.name}}</view>
</view>
